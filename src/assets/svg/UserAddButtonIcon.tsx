import * as React from 'react'
import Svg, { Defs, LinearGradient, Path, Rect, Stop, SvgUri } from 'react-native-svg'

const UserAddButtonIcon = (props) => (
  <SvgUri
    width={40}
    height={20}
    uri='src/assets/svg/user-add.svg'
  />
)

export default UserAddButtonIcon
